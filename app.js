// selectors 
const left = document.querySelector('#left')

// event listeners
left.addEventListener('click', leftReset)

// functions
function leftReset(e) {
    e.target.style.backgroundColor = 'red'
    e.target.style.color = -71 %
        console.log(e)
}